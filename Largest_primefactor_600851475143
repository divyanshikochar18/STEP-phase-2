#include<iostream>
using namespace std;
int main(){
    long long number = 600851475143;
    long long factor = 2;
    while(factor <= number){
        if(number%factor == 0){
            number /= factor;
        }
        else{
            factor++;
        }
    }
    cout << factor<<endl;
}
